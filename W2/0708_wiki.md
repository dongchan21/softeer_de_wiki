## Keep
- 막혔을 때 우회하거나 포기하지 않고, 근본 원인(SCP, 키 페어 불일치 등)을 끝까지 찾아서 해결한 태도

## Problem
- 인스턴스 스펙(t2.nano, 512MB)과 실제 처리할 데이터 크기(160만 행)를 사전에 비교해보지 않고 진행해서 메모리 부족으로 서버가 다운됨
- pandas로 CSV를 읽을 때 필요한 컬럼만 선택하거나 청크 단위로 나누는 등의 메모리 최적화를 코드에 반영하지 않음


## Try
- EC2 인스턴스 생성 전에 키 페어 이름, 저장 위치, 보안 그룹 규칙을 체크리스트로 미리 정리해두고 시작하기
- Docker 컨테이너 실행 시 --memory 옵션으로 리소스 제한을 걸어, 문제가 생겨도 컨테이너만 죽고 EC2 전체가 먹통되는 상황을 방지하기
