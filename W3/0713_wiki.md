Docker로 하둡 멀티노드(namenode+datanode) 클러스터를 직접 구성하고 core/hdfs/mapred/yarn 설정 파일을 과제 스펙대로 수정했다.
SSH가 없는 컨테이너 환경이라 start-dfs.sh 방식이 안 통해서 데몬을 개별적으로 기동해야 했고
ResourceManager 재시작 오류, JAVA_HOME 미설정, root 권한 문제 등을 로그 기반으로 하나씩 원인 추적하며 해결했다.
그 결과 설정 변경부터 재시작, hdfs getconf 등을 통한 실제 적용 검증까지 전체 사이클을 경험할 수 있었다.
